/*
Problem Solving

includes()
*/

let arr = [1,2,3,4];
console.log(arr.includes(3));
console.log(arr.includes(5));

/*
Dry Run:
First call: arr.includes(3)

The method checks if 3 exists in the array.
It finds 3 in the array, so it returns true.
Output: true
Second call: arr.includes(5)

The method checks if 5 exists in the array.
It doesn't find 5, so it returns false.
Output: false
*/